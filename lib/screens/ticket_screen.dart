import 'package:flutter/material.dart';

class TicketScreend extends StatelessWidget {
  const TicketScreend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
